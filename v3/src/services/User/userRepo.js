export default function makeUserRepo({ User }) {
  const listar = async () => {}
  const adicionar = async ({}) => {}
  const getPorId = async ({}) => {}
  const remover = async ({}) => {}

  return Object.freeze({
    listar,
    adicionar,
    getPorId,
    remover,
  })
}
